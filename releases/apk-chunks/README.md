# v1.0.0 APK chunks

Base64 slices of `Skybound-Riftkeepers-v1.0.0.apk`.
When `DONE` is present, GitHub Actions concatenates them into the APK and publishes the v1.0.0 release.

Windows portable zip is larger than the GitHub file cap — use the v1.0.0 pack from the project chat.
