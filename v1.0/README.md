# v1.0.0 — Gate Closed

Current full game.

- Ten worlds, double jump, wrench + gun
- Mobile HUD at the top (score / Nix never sit on the buttons)
- Windows `Skybound.exe` is this same 3D game
- Android APK is this same 3D game

Play from `src/` at the repo root. Do not mix with `v0.1` / `v0.2` Python folders.
